// import React from "react"
import { invoiceList } from "./invoices"
import { projects, projectListData, OverviewTeamMember, projectAssignedTo } from "./projects"
import { tasks, kanbanBoardData, AddTeamMember, recentTasksData } from "./tasks"
import { mailDB, labelsData, mailChatData } from "./mails"
// import { chats, contacts, groups, messages } from "./chat"
import { dashboardEmail, latestTransaction } from "./dashboard"


import { jobs, jobListCandidate, jobApply } from "./job";
import { jobsGridData, experienceData, jobType } from "./job-Grid";

export {
  recentTasksData,
  jobsGridData,
  experienceData,
  jobType,
  AddTeamMember,
  labelsData,
  mailChatData,
  mailDB,
  // chats,
  // groups,
  // contacts,
  // messages,
  invoiceList,
  projects,
  projectListData,
  OverviewTeamMember,
  projectAssignedTo,
  tasks,
  kanbanBoardData,
  dashboardEmail,
  latestTransaction,
  jobs,
  jobListCandidate,
  jobApply,
}
